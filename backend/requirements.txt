fastapi==0.115.12
uvicorn[standard]==0.34.3
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
alembic==1.15.2
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
httpx==0.28.1
python-dotenv==1.1.0
pydantic-settings==2.9.1
email-validator==2.2.0
python-multipart==0.0.20

# Discovery engine — LLM candidate extraction + final evaluation.
# The client is OpenAI-compatible; point LLM_BASE_URL at another provider to swap.
openai>=1.50.0

# Warm cache for discovery. Optional — the app degrades to in-process compute.
redis>=5.0.0,<7.0.0
