# discover — search results support

What survives of the old discovery UI after the rebase.

- **`PeopleResults.tsx`** — cast/crew result rows.

The recommendation rows, "movies like X" rails and Community Consensus panel
were removed with the ML stack. Discovery returns in Phase 8, rendered from the
evidence-first engine, with the evidence visible in the UI rather than an
opaque score.
